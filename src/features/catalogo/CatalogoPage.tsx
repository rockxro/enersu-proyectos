import { PlaceholderPanel } from "@/components/common/PlaceholderPanel";

export function CatalogoPage() {
  return (
    <PlaceholderPanel
      description="CRUD local para productos activos e inactivos del catalogo SQLite."
      eyebrow="Gestion"
      title="Catalogo de productos"
    />
  );
}
