import { PlaceholderPanel } from "@/components/common/PlaceholderPanel";

export function ClienteStep() {
  return (
    <PlaceholderPanel
      description="Formulario para nombre, RUT, direccion, comuna, tarifa y datos comerciales del cliente."
      eyebrow="Paso 1"
      title="Datos del cliente"
    />
  );
}
