import { PlaceholderPanel } from "@/components/common/PlaceholderPanel";

export function ConfiguracionPage() {
  return (
    <PlaceholderPanel
      description="Parametros editables: tarifas por defecto, factor de perdidas, DOD, datos de empresa y datos del PDF."
      eyebrow="Gestion"
      title="Configuracion"
    />
  );
}
